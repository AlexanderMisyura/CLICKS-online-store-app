import { Hero, TopDiscount, TopRated } from "../components";

const HomePage = () => {
  return (
    <main>
      <Hero />
      <TopRated />
      <TopDiscount />
    </main>
  );
};

export default HomePage;
